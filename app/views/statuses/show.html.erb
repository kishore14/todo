<% header = link_to('All Tasks', tasks_path) + " &raquo; #{@status.status}".html_safe %>
<%= render 'shared/content_title', title: header %>

<%= render @status.tasks.where("user_id = ?", @current_user.id) %>
